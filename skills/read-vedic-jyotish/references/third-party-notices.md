# Third-party notices

The bundled local calculator uses `@swisseph/browser` 1.3.1 and
`@swisseph/core` 1.3.1 from the
[swisseph-js project](https://github.com/swisseph-js/swisseph).

Those packages and the bundled Swiss Ephemeris WebAssembly runtime use the
GNU Affero General Public License version 3. Swiss Ephemeris is copyright
Astrodienst AG.

The full license is included as `LICENSE` in this skill directory. Source and
reproducible build inputs are available in the public skill repository.

Open-Meteo geocoding results require attribution to
[Open-Meteo](https://open-meteo.com/).
